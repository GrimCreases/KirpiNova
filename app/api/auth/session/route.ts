import { currentUser } from "@/lib/server/auth/session";export const runtime="nodejs";export const dynamic="force-dynamic";export async function GET(){try{const user=await currentUser();return Response.json({authenticated:Boolean(user),user:user?{email:user.email}:null},{headers:{"Cache-Control":"no-store"}})}catch{return Response.json({authenticated:false,user:null},{headers:{"Cache-Control":"no-store"}})}}
